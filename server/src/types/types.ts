export type TicketRequest = {
  userId: string
  title: string
  description: string
  callMethod: number
}

export type accountCheck = {
  count: number
}

export type accountStatus = {
  premium: number
}
